const today = new Date();
const person = {
  age: 20,
};

class Color {}

const red = new Color();

//Type Annotation: Code to tell typescript what type of value a variable will refer to.
//Type Inference: Typescript figures out what type a variable value refers to.
